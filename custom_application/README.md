# Custom application

Custom integration (based on secure_storage) to be inlined with upstream optee implementation
to fit only application needs.
It includes TA (trusted application) and CA (custom application in linux (non secure world)).

Provides secure tools for passwords and LUKS.